function updateTime() {
    // new Date().toLocaleTimeString() → Gets the current time. 
    //document.getElementById("currentTime").innerText = ... → Updates the time display every second.
    document.getEleme